apiVersion: clickhouse.com/v1alpha1
kind: ClickHouseCluster
metadata:
  name: sample
spec:
  replicas: 2
  dataVolumeClaimSpec:
    resources:
      requests:
        storage: 1Gi
  keeperClusterRef:
    name: sample
